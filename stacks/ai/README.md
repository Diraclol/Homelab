# ai

Ollama + Open WebUI. The compose file must include the NVIDIA runtime / GPU reservation —
that is the fix from the incident log, so keep it visible.

Files to add here from the host: `compose.yaml`.
